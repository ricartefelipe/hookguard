databaseChangeLog:
  - changeSet:
      id: 004-totalrecall-password
      author: hookguard
      changes:
        - addColumn:
            tableName: accounts
            columns:
              - column: { name: password_hash, type: varchar(255) }
              - column: { name: enabled, type: boolean, defaultValueBoolean: true, constraints: { nullable: false } }
              - column: { name: expires_at, type: timestamptz }
