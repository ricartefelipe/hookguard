spring:
  application:
    name: hookguard-api
  datasource:
    url: ${HOOKGUARD_DB_URL:jdbc:postgresql://localhost:5432/hookguard}
    username: ${HOOKGUARD_DB_USER:hookguard}
    password: ${HOOKGUARD_DB_PASSWORD:hookguard}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
  mail:
    host: ${HOOKGUARD_SMTP_HOST:localhost}
    port: ${HOOKGUARD_SMTP_PORT:1025}
    username: ${HOOKGUARD_SMTP_USER:}
    password: ${HOOKGUARD_SMTP_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: ${HOOKGUARD_SMTP_AUTH:false}
          starttls:
            enable: ${HOOKGUARD_SMTP_STARTTLS:false}

server:
  port: ${HOOKGUARD_PORT:8080}
  forward-headers-strategy: framework

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true

hookguard:
  worker:
    enabled: ${HOOKGUARD_WORKER_ENABLED:true}
    poll-interval-ms: ${HOOKGUARD_WORKER_POLL_MS:1000}
    batch-size: ${HOOKGUARD_WORKER_BATCH:20}
    lock-timeout-seconds: ${HOOKGUARD_WORKER_LOCK_TIMEOUT:60}
  delivery:
    connect-timeout-ms: 3000
    default-timeout-ms: 10000
    max-attempts: 6
    allow-http: ${HOOKGUARD_DELIVERY_ALLOW_HTTP:false}
  retention:
    days: 14
  rate-limit:
    ingest-per-minute: ${HOOKGUARD_INGEST_RATE_LIMIT:120}
  billing:
    free-monthly-events: 5000
    pro-monthly-events: 100000
    business-monthly-events: 1000000
    stripe-api-key: ${STRIPE_API_KEY:}
    stripe-webhook-secret: ${STRIPE_WEBHOOK_SECRET:}
    stripe-pro-price-id: ${STRIPE_PRO_PRICE_ID:}
    stripe-business-price-id: ${STRIPE_BUSINESS_PRICE_ID:}
    stripe-meter-event-name: ${STRIPE_METER_EVENT_NAME:}
  cors:
    allowed-origins: ${HOOKGUARD_CORS_ORIGINS:http://localhost:3000}
  auth:
    app-base-url: ${HOOKGUARD_APP_BASE_URL:http://localhost:3000}
    api-base-url: ${HOOKGUARD_API_BASE_URL:http://localhost:8080}
    from-email: ${HOOKGUARD_FROM_EMAIL:noreply@hookguard.local}
    magic-link-ttl-minutes: 20
    session-ttl-days: 30
    expose-magic-link: ${HOOKGUARD_EXPOSE_MAGIC_LINK:false}
    trust-forwarded-host: ${HOOKGUARD_TRUST_FORWARDED_HOST:false}
    github-client-id: ${GITHUB_CLIENT_ID:}
    github-client-secret: ${GITHUB_CLIENT_SECRET:}
logging:
  level:
    root: INFO
    br.com.ricarte.hookguard: INFO
